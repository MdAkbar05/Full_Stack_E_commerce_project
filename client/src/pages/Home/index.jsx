import React, { useEffect, useState } from "react";
import useFetch from "./../../Helpers/AxiosApi";
const Home = () => {
  // const { isLoading, isError, data } = useFetch(
  //   "http://localhost:3000/api/users"
  // );
  // const users = data;

  return <div>Welcome home</div>;
};

export default Home;
